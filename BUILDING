how to build:
 
 git submodule init
 git submodule update
 cd src
 rm -rf build && mkdir build && cd build
 cmake ..
 make  (devenv OpenContainingFolderService.sln on Windows)
 
A dir OpenContainingFolder should be generated which is your service ready to 
be installed using the BrowserPlus SDK:
 
http://browserplus.yahoo.com/developer/service/sdk/
